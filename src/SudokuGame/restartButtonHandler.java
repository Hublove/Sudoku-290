package SudokuGame;

import javafx.event.ActionEvent;
import javafx.event.EventHandler;

/**
 * the handler of the restart button
 * move to the instruction page
 * @return none
 * 		the button that show the main page to player when in
 * 		they hit on it
 */

public class restartButtonHandler implements EventHandler<ActionEvent> {
	
	@Override
	//handle when restart button is being pressed
	public void handle(ActionEvent event) {
		// TODO Auto-generated method stub
		
	}

}
